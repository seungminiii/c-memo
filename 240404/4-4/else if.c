#include <stdio.h>

int main() {
	// if - else if - else
	// else if, else를 사용하려면, if가 있어야 함(if 생략불가)
	
	// if문 :: "만약에~", 조건식에 따라 수행할 명령문을 달리할 때 사용
	// if (조건식1) { 조건식 1이 성립할 때 수행할 명령문 나열; }

	// else if문 :: "위 조건문을 만족하지 않고 이 조건문을 만족할 때"
	//				 수행할 명령문 작성할 때 사용
	// else if (조건식2) { 조건식1이 성립되지 않고, 
	//				       조건식 2가 성립될 때 수행할 명령문 나열}

	// else문 :: "위 조건문(들)을 만족하지 않을 때" 수행할 명령문
	// else { 위 조건식을 성립하지 않을 때 수행할 명령문 나열; }

}